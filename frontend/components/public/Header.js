'use client';

import Navbar from './Navbar';

/**
 * Header component que contiene el navbar principal
 * Equivalente al Footer para mantener consistencia en la estructura
 */
export default function Header() {
  return (
    <header>
      <Navbar />
    </header>
  );
}