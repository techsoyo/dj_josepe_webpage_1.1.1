// services/dashboardService.js
//
// Provides endpoints for the admin dashboard.
// These endpoints are protected by authentication and the api instance 
// automatically handles injecting the bearer token.

import api from './api';

const dashboardService = {
  // Dashboard service functions can be added here as needed
};

export default dashboardService;